# Ambiente oficial de build/assinatura/publicacao (Python 3.11.0, PyInstaller 6.21.0).
# Versoes pinadas conforme o ambiente aprovado (o release.py verifica e falha com
# diagnostico se o Python ou o PyInstaller divergirem — ver AGENTS.md).
# Build/empacotamento:
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
altgraph==0.17.5
pefile==2024.8.26
pywin32-ctypes==0.2.3
# Runtime do app:
pillow==12.3.0
pypdfium2==5.13.0
sounddevice==0.5.5
websocket-client==1.9.0
pywin32==311
# Assinatura do manifesto (release.py):
cryptography==50.0.0
# Publicação incremental no Cloudflare R2:
boto3==1.43.64
